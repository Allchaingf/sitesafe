//
//  ContentView.swift
//  SiteSafe
//
//  Thin entry wrapper kept for the default scaffold reference. The real entry
//  flow lives in RootView (Splash → Onboarding → Main).
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        RootView()
    }
}
